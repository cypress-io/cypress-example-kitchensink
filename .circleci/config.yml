# The Cypress tests for this repository run in GitHub Actions.
# See .github/workflows/ for the live test workflows.
#
# basic/.circleci/config.yml shows Cypress running on CircleCI, though without
# the Cypress orb. The orb itself is documented at
# https://on.cypress.io/circleci-orb
#
# This configuration publishes the npm package with semantic-release, and uses
# the orb only for its executor.

# to use CircleCI orbs need to use version >= 2.1
version: 2.1
orbs:
  # use Cypress orb from CircleCI registry
  cypress: cypress-io/cypress@6.1.0 # https://github.com/cypress-io/circleci-orb/releases

jobs:
  release:
    executor:
      name: cypress/default
      node-version: '24.18.0'
    steps:
      - checkout
      - run: npm ci
      - run: npx semantic-release@25.0.3

workflows:
  publish:
    jobs:
      - release:
          context: org-npm-credentials
          filters:
            branches:
              only:
                - master
