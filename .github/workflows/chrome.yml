name: Chrome

on: push

jobs:
  'chrome run':
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v1
      - name: Chrome
        uses: cypress-io/github-action@v1
        timeout-minutes: 10
        with:
          build: npm run build
          start: npm start
          browser: chrome
